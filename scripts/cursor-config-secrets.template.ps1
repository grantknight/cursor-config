# Copy to ~/.cursor-config-secrets.ps1 (never commit)
# Or set the same names as Windows User environment variables.

# $TELEGRAM_BOT_TOKEN = 'from BotFather / Railway hermes-agent'
# $TELEGRAM_CHAT_ID = '-1004292432894'
# $TELEGRAM_TOPIC_ID = ''  # optional Knight HQ forum topic for Cursor alerts
